#include <stdio.h>
/**
 *main - print all one digit numbers
 *
 *Return: Always (Success)
 */
int main(void)
{
char number;
for (number = 0; number <= 9; number++)
{
printf("%d", number);
}
return (0);
}
