#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * Return: a number with the absolute value of an integer
 */
int _abs(int n)
{
int n;
if (n >=  0)
return (n);
else
return (-n);
}
