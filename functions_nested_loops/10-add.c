#include "main.h"
/**
 *add - adds two integers and returns the result
 *@a: first integer to be added
 *@b: second integer to be added
 *
 * Return: the result of adding a and b integers
 */
int add(int a, int b)
{
return (a + b);
}
